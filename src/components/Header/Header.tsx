import UserBar from '../UserBar/UserBar';
import { HeaderContainer } from './Header.styled';

const Header = () => {
  return (
    <HeaderContainer>
      <UserBar />
    </HeaderContainer>
  );
};

export default Header;
